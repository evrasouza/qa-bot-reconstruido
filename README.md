# QA Bot

Crawler + Playwright test generation framework for large-scale page health validation.

## Quick start

```bash
npm install
npx playwright install
npm run crawl
npm run generate
npm run test
npm run report:allure
```
